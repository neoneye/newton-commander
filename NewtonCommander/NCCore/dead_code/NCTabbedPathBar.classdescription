{
	Actions = {
		// Define action descriptions here, for example
		// "myAction:" = id;
		"newTab:" = id;
	};
	Outlets = {
		// Define outlet descriptions here, for example
		// myOutlet = NSView;
	};
    ClassName = NCTabbedPathBar; 
    SuperClass = NSView; 
}
